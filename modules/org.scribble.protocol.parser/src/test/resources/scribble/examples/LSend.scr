module scribble.examples.LSend;

local protocol LSendTest at Buyer(role Seller) {
	buy(Order,Customer) to Seller;
	buy(o:Order,c:Customer) to Seller;
	buy(Order) to Seller;
	buy(ord:Order) to Seller;
}
