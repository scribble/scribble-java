//Raymond@HZHL2 ~/code/python/scribble-tools
//$ PYTHONPATH='lib/:bin/' python src/scribble/Main.py -ip test/ test/base/impourt/pack1/Pack14.scr

module good.misc.imports.pack1.Pack14;

import good.misc.imports.Import14;


global protocol Pack14<sig X>(role C, role D)
{
	do good.misc.imports.Import14.Import14Aux<X>(C, D);
}

