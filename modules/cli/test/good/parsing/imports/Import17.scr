//Raymond@HZHL2 ~/code/python/scribble-tools
//$ PYTHONPATH='lib/:bin/' python src/scribble/Main.py -ip test/ test/base/impourt/Import17.scr

module parsing.imports.Import17;

import parsing.imports.Import17; // OK because full name is a "compound" name -- if simple and full names coincide for a module, self import is not OK
