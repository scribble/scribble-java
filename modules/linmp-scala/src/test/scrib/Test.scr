module Test;

type <java> "java.lang.Integer" from "rt.jar" as Int;

global protocol Proto(role A, role B)
{
	choice at A
	{
		1(Int) from A to B;
		3(Int) from B to A;
	}
	or
	{
		2(Int) from A to B;
	}
}
