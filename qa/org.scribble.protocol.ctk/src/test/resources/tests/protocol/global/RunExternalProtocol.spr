import Order;
import Confirm;
import protocol Sub from "ReferencedExternalProtocol.spr";

protocol RunSubProtocol (role Buyer) {
	Buyer introduces Seller;

	run Sub(Buyer, Seller) from Buyer;

	Confirm from Seller to Buyer;
}
