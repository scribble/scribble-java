import Order;
import Cancel;
import Invoice;

protocol EndExample at Buyer {
	Buyer introduces Seller;

	choice at Buyer {
		Order to Seller;
	} or {
		Cancel to Seller;
		end;
	}

	Invoice from Seller;
}
