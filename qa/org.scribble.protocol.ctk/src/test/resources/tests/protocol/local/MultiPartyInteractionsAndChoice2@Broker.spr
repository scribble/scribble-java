import Order;
import Confirmation;
import OutOfStock;
import InvalidProduct;

protocol MultiPartyInteractionsAndChoice2@Broker(role Buyer) {
	Broker introduces Seller;

	Order from Buyer;
	
	choice at Broker {
		Order to Seller;
	
		choice {
			Confirmation from Seller;
			Confirmation to Buyer;
		} or {
			OutOfStock from Seller;
			OutOfStock to Buyer;
		}
	} or {
		invalidProduct(InvalidProduct) to Buyer;
	}
}
