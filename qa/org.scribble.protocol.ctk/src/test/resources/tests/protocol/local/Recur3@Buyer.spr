import Order;
import Finish;
import Rejected;
import Accepted;
import Another;

protocol Recur@Buyer {
	role Seller;

	rec Transaction {
		Order to Seller;
		
		choice {
			tryAgain() from Seller;
			Transaction;
		} or {
			exit() from Seller;
		}
		
		Another to Seller;
	}
	
	Finish to Seller;
}
