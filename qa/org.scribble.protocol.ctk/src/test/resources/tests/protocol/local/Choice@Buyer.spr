import Order;
import Cancel;
import Invoice;

protocol SingleInteraction@Buyer {
	Buyer introduces Seller;

	choice at Buyer {
		Order to Seller;
		Invoice from Seller;
	} or {
		Cancel to Seller;
	}
}
