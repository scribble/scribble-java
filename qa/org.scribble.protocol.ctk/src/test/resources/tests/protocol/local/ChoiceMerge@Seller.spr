import Order;
import Cancel;
import Invoice;
import Cancelled;

protocol ChoiceMerge at Seller (role Broker) {

	choice {
		Order from Broker;
		Invoice to Broker;
	} or {		
		Cancel from Broker;
		Cancelled to Broker;
	}
}
