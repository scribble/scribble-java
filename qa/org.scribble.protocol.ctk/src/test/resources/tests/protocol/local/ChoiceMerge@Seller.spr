import Order;
import Cancel;
import Invoice;
import Cancelled;

protocol ChoiceMerge@Seller {
	role Broker;

	choice {
		Order from Broker;
		Invoice to Broker;
	} or {		
		Cancel from Broker;
		Cancelled to Broker;
	}
}
